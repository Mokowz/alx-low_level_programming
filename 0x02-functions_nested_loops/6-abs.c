#include "main.h"

/**
 * _abs - Compute absolute value of a number
 * @n: Integer 1
 * Description: Give integer return absolute value
 *
 * Return: Number
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
